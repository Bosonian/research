<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to drive the device vibration motor. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- The app needs a vibrator to be useful, but should still install on
         devices without one so it can report the missing capability. -->
    <uses-feature
        android:name="android.hardware.vibrator"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vibration128">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ThresholdActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />

        <!-- Runs in its own process so the error panel survives the crash that
             opened it (the crashing app process is torn down by App). -->
        <activity
            android:name=".ErrorConsoleActivity"
            android:exported="false"
            android:process=":crash"
            android:screenOrientation="portrait" />
    </application>
</manifest>
